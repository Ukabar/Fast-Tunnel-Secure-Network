class AdScreenIds {
  const AdScreenIds._();

  static const home = 'home';
  static const locations = 'locations';
  static const history = 'history';
  static const settings = 'settings';

  static const supportedBannerScreens = {home, locations, history, settings};
}
