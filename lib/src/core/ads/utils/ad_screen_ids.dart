class AdScreenIds {
  const AdScreenIds._();

  static const home = 'home';
  static const history = 'history';
  static const settings = 'settings';

  static const supportedBannerScreens = {home, history, settings};
}
