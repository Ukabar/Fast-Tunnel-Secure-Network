class AdScreenIds {
  const AdScreenIds._();

  static const locations = 'locations';
  static const history = 'history';
  static const settings = 'settings';

  static const supportedBannerScreens = {locations, history, settings};
}
